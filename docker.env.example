# Copy to `.env` in this directory so Docker Compose can substitute variables:
#   cp docker.env.example .env
# Edit JWT_SECRET to a long random string before production.

JWT_SECRET=
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432
APP_PORT=3000
